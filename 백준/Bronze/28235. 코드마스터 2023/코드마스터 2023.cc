#include <iostream>

using namespace std;

int main()
{
    string str;
    cin >> str;
    if (str[0] == 'S') cout << "HIGHSCHOOL";
    if (str[0] == 'C') cout << "MASTER";
    if (str[0] == '2') cout << "0611";
    if (str[0] == 'A') cout << "CONTEST";
}