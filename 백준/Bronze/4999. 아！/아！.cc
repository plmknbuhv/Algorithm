#include <iostream>
#include <cmath>

using namespace std;
int result = 0;


int main()
{
    string a, b;
    cin >> a >> b;

    if (a > b)
        cout << "no";
    else
        cout << "go";

    return 0;
}